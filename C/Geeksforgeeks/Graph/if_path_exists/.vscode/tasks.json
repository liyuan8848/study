{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build app",
            "type": "shell",
            "command": "g++",
            "args": ["-g", "-o", "if_path_exist_main", "if_path_exist_main.c"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}